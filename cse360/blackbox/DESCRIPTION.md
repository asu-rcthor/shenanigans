If you do not wish to download the jar (as source code is not given), run it here in this challenge.

Use the following command to run the challenge:
```sh
java -jar /challenge/UserNameValidator.jar
```
